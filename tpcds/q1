select max(tot), min(tot), count(1)
from (
  select sr_store_sk, count(1) as tot
  from store_returns, date_dim
  WHERE sr_returned_date_sk = d_date_sk
       AND d_year = 2001
  group by sr_store_sk) t
